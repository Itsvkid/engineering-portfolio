FoamFile{version 2.0; format ascii; class volScalarField; object T;}
dimensions [0 0 0 1 0 0 0];
internalField uniform 288.15;
boundaryField
{
    inlet   { type totalTemperature; gamma 1.4; T0 uniform 288.15; value uniform 288.15; }
    outlet  { type inletOutlet; inletValue uniform 288.15; value uniform 288.15; }
    hub_rotating     { type zeroGradient; }
    hub_static        { type zeroGradient; }
    blade   { type zeroGradient; }
    casing  { type zeroGradient; }
    periodic_m { type cyclic; }
    periodic_p { type cyclic; }
}
